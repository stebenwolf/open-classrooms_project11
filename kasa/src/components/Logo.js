import React from "react";
import logo from "../assets/logo.svg";

const Logo = () => {
    return (
        <div>
            <img src={logo} alt="logo" />
        </div>
    );
}

export default Logo;